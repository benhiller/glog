{
  "name": "@benhiller/glog",
  "version": "0.1.5",
  "bin": {
    "glog": "bin/glog.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node bin/glog.js",
    "build": "rm -rf dist .parcel-cache && parcel build src/index.html --dist-dir dist",
    "dev": "rm -rf dist .parcel-cache && parcel src/index.html --dist-dir dist"
  },
  "keywords": [
    "logging",
    "stream",
    "web",
    "terminal"
  ],
  "author": "",
  "license": "ISC",
  "description": "Stream terminal input to a live-updating web interface",
  "dependencies": {
    "express": "^4.18.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "parcel": "^2.15.2"
  },
  "publishConfig": {
    "access": "public"
  }
}
